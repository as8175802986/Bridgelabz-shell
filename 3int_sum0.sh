#!/bin/bash -x
arr =[0, -1, 2, -3, 1];
function findtriplets(arr){
	let found = false;
	for (i=0; i<=arr.length;i++){
	for (j=1; J<=arr.length;j++){
	for (k=2; k<=arr.length;j++){
if ($arr[i] + $arr[j] + $arr[k] == 0)
{
document.write($arr[i]);
                    document.write(" ");
                    document.write($arr[j]);
                    document.write(" ");
                    document.write($arr[k]);
                    document.write("<br>");
                    found = true;
echo {$document.write[@]}
                    }
	}
}
if(found == false) {
	documentwrite("not exist");
}
}
}
$findtriplets(arr) 
